# html5-games

Self-contained site that lists and hosts small HTML5 canvas games.

How to run (from repository root):

```
# start the server and visit http://localhost:3000/html5-games
npm run start
```

The demos are in `js/games/` and include `neon.js` and `pixel.js`.
